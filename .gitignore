# Dependencies
node_modules/

# Environment variables
.env
.env.local
.env.production

# Build output
dist/

# SQLite database files (runner local storage)
*.db
*.db-shm
*.db-wal

# Runner stable identity (generated per host at first start)
runner-id

# Claude Code — personal/generated only; settings.json, hooks/, rules/, agents/ are committed
.claude/settings.local.json
.claude/worktrees/
.claude/logs/hooks*.log
.claude/current-brief.md

# Smoke test runtime — backgrounded process logs/pids
.smoke/

# OS files
.DS_Store
Thumbs.db
Desktop.ini
